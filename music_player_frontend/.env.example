# Frontend runtime configuration (Create React App)
#
# This app is anonymous/public by default (no authentication, no tokens).
#
# The frontend API client reads the backend base URL from REACT_APP_API_BASE_URL
# (see src/api/client.js).
#
# Local dev backend:
#   http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001
